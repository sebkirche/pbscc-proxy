
```
[config]
; lock strategy: 
;   "lock"  using svn lock (default from version 2.x)
;   "prop"  using svn properties (old behavior)
lock.strategy=prop

;the only valid values are "utf-8" and "none"
export.encoding=utf-8

;prepend commit message prefix (false by default)
message.prefix=true
```